# 2026-07-06 GUI 事件注入验证

## 实现结果

- 新增统一事件状态、事件效果、GUI 可视化、动态选路和 CSV 日志模块。
- 支持早晚高峰、演唱会、道路施工、恶劣天气，以及原有事故、封路、大型活动配置。
- 事件时间可按 demo 启动后的相对秒数调度。
- 同一道路或车辆受多个事件影响时采用叠加状态，最后一个事件结束后恢复原始值。

## 自动测试

- `python -m unittest discover -s tests -v`
- 结果：4 项测试全部通过。
- 覆盖：叠加限速恢复、车道权限恢复、无效道路替换、GUI API 缺失降级。

## 800 秒无界面集成测试

- 5 个事件全部正常开始和结束。
- 生成车辆：早高峰 60、演唱会 70、晚高峰 60，共 190 辆。
- 记录交通指标 160 行。
- 车辆峰值约 125 辆。
- 拥堵阶段平均速度最低约 6.59 m/s。
- 施工和台风事件均完成车道速度、权限和车辆状态恢复。

## GUI 冒烟测试

- `sumo-gui` 正常启动并运行至演唱会事件窗口。
- POI、polygon 和自动镜头聚焦 TraCI 调用均被 GUI 接受。
- 测试过程未触发 GUI API 降级 warning。

## GUI 观察速度调整

- GUI 专用入口默认由 `20 ms/步` 调整为 `200 ms/步`，800 步约需 2 分 40 秒。
- 每个事件开始时额外暂停 3 秒。
- 自动聚焦由 18 个仿真步延长至 60 个仿真步，默认约可观察 12 秒。
- 无界面测试保持 `0 ms/步`，不降低自动验证效率。
- 修复短演示结束后 `sumo-gui` 子进程残留的问题。

## GUI 可见性修复

- 根据实际截图确认，原实心事件 polygon 会遮挡道路和车辆。
- 事件区域改为空心边界，并使用粗彩色线条直接标记受影响道路。
- 自动缩放调整为适合观察道路与车辆的倍率。
- 动态事件先生成车辆，等待 GUI 刷新一个仿真步后再暂停。
- 镜头跟随一辆真实事件车辆，并在车辆周围绘制约 40 米宽的随车空心菱形。
- 施工、天气和事故事件激活时优先选择当时实际有车辆的道路。
- 自动保存 `03_Outputs/events/screenshots/*_focus.png` 作为可见性验证。
- 根据最终展示需求，已移除 GUI 中的所有事件文字；GUI 只保留真实交通状态和必要的道路/车辆视觉提示，事件解释统一输出到后台。
- 修复车辆已注册但尚未进入路网时返回 `(-1073741824, -1073741824)` 哨兵坐标，导致镜头跳出地图并出现 `Point outside of projection domain` 的问题。
- 所有车辆跟踪坐标现在必须通过实际路网边界校验；无效时保持事件道路中心，等待有效车辆位置。
- 自动事件位置改为每次运行重新随机抽取；施工、天气和事故会从当次仿真的繁忙道路候选池中加权选择。
- 后台统一输出事件时刻、事件类型、道路/车道位置、交通现象和恢复结果，并记录选址随机种子。
